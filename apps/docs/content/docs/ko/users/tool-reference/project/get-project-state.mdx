---
title: 프로젝트 상태 조회
description: "프로젝트 상태 조회 도구 레퍼런스. 입력 스키마, 필드 제약, 요청/응답 예시를 제공합니다."
---

## 빠른 요약
현재 프로젝트의 구조, 텍스처, 애니메이션 상태를 읽기 전용으로 조회합니다.

## 개요
- 도구 이름: `get_project_state`
- 카테고리: 프로젝트
- Strict schema: `true`

## 도구 설명
현재 프로젝트의 구조, 텍스처, 애니메이션 상태를 읽기 전용으로 조회합니다.

## 입력 스키마 (JSON)
```json
{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "detail": {
      "type": "string",
      "enum": [
        "summary",
        "full"
      ]
    },
    "includeUsage": {
      "type": "boolean",
      "description": "Include textureUsage in the response (defaults to true when detail=full)."
    }
  }
}
```

## 필드 레퍼런스
| Field | Type | 필수 | 설명 | 제약 |
| --- | --- | --- | --- | --- |
| `detail` | `enum("summary", "full")` | ❌ | - | enum: "summary", "full" |
| `includeUsage` | `boolean` | ❌ | Include textureUsage in the response (defaults to true when detail=full). | - |

## 최소 요청 예시
```json
{}
```

## 응답 예시
```json
{
  "structuredContent": {
    "project": {
      "id": "value",
      "active": false,
      "name": "value",
      "format": "Java Block/Item",
      "revision": "value",
      "counts": {
        "bones": 0,
        "cubes": 0,
        "textures": 0,
        "animations": 0
      }
    }
  }
}
```

## 운영 노트
- 변경 도구는 `ifRevision` 사용을 권장합니다.
- 스키마에 없는 필드는 보내지 마세요 (strict schema).
- `validate`로 최종 상태를 확인한 후 `export`를 수행하세요.
