---
title: Update Bone
description: "Updates a bone by id/name."
---

## Quick Summary
Updates a bone by id/name.

## Overview
- Tool name: `update_bone`
- Category: Modeling
- Strict schema: `true`

## Tool Description
Updates a bone by id/name.

## Input Schema (JSON)
```json
{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string"
    },
    "name": {
      "type": "string"
    },
    "newName": {
      "type": "string"
    },
    "parent": {
      "type": "string"
    },
    "parentId": {
      "type": "string"
    },
    "parentRoot": {
      "type": "boolean"
    },
    "pivot": {
      "type": "array",
      "items": {
        "type": "number"
      },
      "minItems": 3,
      "maxItems": 3
    },
    "rotation": {
      "type": "array",
      "items": {
        "type": "number"
      },
      "minItems": 3,
      "maxItems": 3
    },
    "scale": {
      "type": "array",
      "items": {
        "type": "number"
      },
      "minItems": 3,
      "maxItems": 3
    },
    "visibility": {
      "type": "boolean"
    },
    "ifRevision": {
      "type": "string"
    },
    "includeState": {
      "type": "boolean"
    },
    "includeDiff": {
      "type": "boolean"
    },
    "diffDetail": {
      "type": "string",
      "enum": [
        "summary",
        "full"
      ]
    }
  }
}
```

## Field Reference
| Field | Type | Required | Description | Constraints |
| --- | --- | --- | --- | --- |
| `id` | `string` | ❌ | Not specified in schema. | No additional constraints declared. |
| `name` | `string` | ❌ | Not specified in schema. | No additional constraints declared. |
| `newName` | `string` | ❌ | Not specified in schema. | No additional constraints declared. |
| `parent` | `string` | ❌ | Not specified in schema. | No additional constraints declared. |
| `parentId` | `string` | ❌ | Not specified in schema. | No additional constraints declared. |
| `parentRoot` | `boolean` | ❌ | Not specified in schema. | No additional constraints declared. |
| `pivot` | `array<number>` | ❌ | Not specified in schema. | minItems: 3, maxItems: 3 |
| `rotation` | `array<number>` | ❌ | Not specified in schema. | minItems: 3, maxItems: 3 |
| `scale` | `array<number>` | ❌ | Not specified in schema. | minItems: 3, maxItems: 3 |
| `visibility` | `boolean` | ❌ | Not specified in schema. | No additional constraints declared. |
| `ifRevision` | `string` | ❌ | Not specified in schema. | No additional constraints declared. |
| `includeState` | `boolean` | ❌ | Not specified in schema. | No additional constraints declared. |
| `includeDiff` | `boolean` | ❌ | Not specified in schema. | No additional constraints declared. |
| `diffDetail` | `enum("summary", "full")` | ❌ | Not specified in schema. | enum: "summary", "full" |

## Minimal Request Example
```json
{
  "name": "bone"
}
```

## Response Example
This response is a schematic example. Actual values and optional fields vary by project state and runtime version.

```json
{
  "structuredContent": {
    "id": "value",
    "name": "value"
  }
}
```

## Operational Notes
- Send only fields declared in the input schema.
- Treat the response example as schematic, and handle optional fields defensively.
- For mutating tools, use `ifRevision` when the tool supports it.
