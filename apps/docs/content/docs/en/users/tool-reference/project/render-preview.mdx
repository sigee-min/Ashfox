---
title: Render Preview
description: "Renders a preview image. fixed -> single (optional angle). turntable -> sequence. Returns MCP image content blocks (base64 PNG) plus structured metadata without dataUri. Set saveToTmp=true to write snapshots into .ashfox/tmp for manual upload fallback. See ashfox://guide/vision-fallback via resources/read. Single returns result.image; sequence returns result.frames[]. Example(single): \\{\"mode\":\"fixed\",\"output\":\"single\",\"angle\":[30,45,0]\\} Example(sequence): \\{\"mode\":\"turntable\",\"output\":\"sequence\",\"durationSeconds\":2,\"fps\":12\\}"
---

## Quick Summary
Renders a preview image. fixed -> single (optional angle). turntable -> sequence. Returns MCP image content blocks (base64 PNG) plus structured metadata without dataUri. Set saveToTmp=true to write snapshots into .ashfox/tmp for manual upload fallback. See ashfox://guide/vision-fallback via resources/read. Single returns result.image; sequence returns result.frames[]. Example(single): \{"mode":"fixed","output":"single","angle":[30,45,0]\} Example(sequence): \{"mode":"turntable","output":"sequence","durationSeconds":2,"fps":12\}

## Overview
- Tool name: `render_preview`
- Category: Project
- Strict schema: `true`

## Tool Description
Renders a preview image. fixed -> single (optional angle). turntable -> sequence. Returns MCP image content blocks (base64 PNG) plus structured metadata without dataUri. Set saveToTmp=true to write snapshots into .ashfox/tmp for manual upload fallback. See ashfox://guide/vision-fallback via resources/read. Single returns result.image; sequence returns result.frames[]. Example(single): \{"mode":"fixed","output":"single","angle":[30,45,0]\} Example(sequence): \{"mode":"turntable","output":"sequence","durationSeconds":2,"fps":12\}

## Input Schema (JSON)
```json
{
  "type": "object",
  "required": [
    "mode"
  ],
  "additionalProperties": false,
  "properties": {
    "mode": {
      "enum": [
        "fixed",
        "turntable"
      ]
    },
    "angle": {
      "type": "array",
      "items": {
        "type": "number"
      },
      "minItems": 2,
      "maxItems": 3
    },
    "clip": {
      "type": "string"
    },
    "timeSeconds": {
      "type": "number"
    },
    "durationSeconds": {
      "type": "number"
    },
    "fps": {
      "type": "number"
    },
    "output": {
      "enum": [
        "single",
        "sequence"
      ]
    },
    "saveToTmp": {
      "type": "boolean"
    },
    "tmpName": {
      "type": "string"
    },
    "tmpPrefix": {
      "type": "string"
    },
    "includeState": {
      "type": "boolean"
    },
    "ifRevision": {
      "type": "string"
    }
  }
}
```

## Field Reference
| Field | Type | Required | Description | Constraints |
| --- | --- | --- | --- | --- |
| `mode` | `enum("fixed", "turntable")` | ✅ | Not specified in schema. | enum: "fixed", "turntable" |
| `angle` | `array<number>` | ❌ | Not specified in schema. | minItems: 2, maxItems: 3 |
| `clip` | `string` | ❌ | Not specified in schema. | No additional constraints declared. |
| `timeSeconds` | `number` | ❌ | Not specified in schema. | No additional constraints declared. |
| `durationSeconds` | `number` | ❌ | Not specified in schema. | No additional constraints declared. |
| `fps` | `number` | ❌ | Not specified in schema. | No additional constraints declared. |
| `output` | `enum("single", "sequence")` | ❌ | Not specified in schema. | enum: "single", "sequence" |
| `saveToTmp` | `boolean` | ❌ | Not specified in schema. | No additional constraints declared. |
| `tmpName` | `string` | ❌ | Not specified in schema. | No additional constraints declared. |
| `tmpPrefix` | `string` | ❌ | Not specified in schema. | No additional constraints declared. |
| `includeState` | `boolean` | ❌ | Not specified in schema. | No additional constraints declared. |
| `ifRevision` | `string` | ❌ | Not specified in schema. | No additional constraints declared. |

## Minimal Request Example
```json
{
  "mode": "fixed"
}
```

## Response Example
This response is a schematic example. Actual values and optional fields vary by project state and runtime version.

```json
{
  "structuredContent": {
    "kind": "single",
    "frameCount": 0
  }
}
```

## Operational Notes
- Send only fields declared in the input schema.
- Treat the response example as schematic, and handle optional fields defensively.
- For mutating tools, use `ifRevision` when the tool supports it.
