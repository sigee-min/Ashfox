---
title: Ensure Project
description: "Ensures a usable project. Reuses the active project by default and can create a new one when missing or on mismatch (per options). Use match/onMismatch/onMissing to control behavior. action=\"delete\" closes the active project (requires target.name)."
---

## Quick Summary
Ensures a usable project. Reuses the active project by default and can create a new one when missing or on mismatch (per options). Use match/onMismatch/onMissing to control behavior. action="delete" closes the active project (requires target.name).

## Overview
- Tool name: `ensure_project`
- Category: Project
- Strict schema: `true`

## Tool Description
Ensures a usable project. Reuses the active project by default and can create a new one when missing or on mismatch (per options). Use match/onMismatch/onMissing to control behavior. action="delete" closes the active project (requires target.name).

## Input Schema (JSON)
```json
{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "action": {
      "type": "string",
      "enum": [
        "ensure",
        "delete"
      ]
    },
    "target": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string"
        }
      }
    },
    "format": {
      "type": "string",
      "enum": [
        "Java Block/Item",
        "geckolib",
        "animated_java",
        "Generic Model"
      ]
    },
    "name": {
      "type": "string"
    },
    "match": {
      "type": "string",
      "enum": [
        "none",
        "format",
        "name",
        "format_and_name"
      ]
    },
    "onMismatch": {
      "type": "string",
      "enum": [
        "reuse",
        "error",
        "create"
      ]
    },
    "onMissing": {
      "type": "string",
      "enum": [
        "create",
        "error"
      ]
    },
    "confirmDiscard": {
      "type": "boolean"
    },
    "uvPixelsPerBlock": {
      "type": "number"
    },
    "dialog": {
      "type": "object",
      "additionalProperties": true
    },
    "force": {
      "type": "boolean"
    },
    "ifRevision": {
      "type": "string"
    },
    "includeState": {
      "type": "boolean"
    },
    "includeDiff": {
      "type": "boolean"
    },
    "diffDetail": {
      "type": "string",
      "enum": [
        "summary",
        "full"
      ]
    }
  }
}
```

## Field Reference
| Field | Type | Required | Description | Constraints |
| --- | --- | --- | --- | --- |
| `action` | `enum("ensure", "delete")` | ❌ | Not specified in schema. | enum: "ensure", "delete" |
| `target` | `object` | ❌ | Not specified in schema. | additionalProperties=false |
| `target.name` | `string` | ❌ | Not specified in schema. | No additional constraints declared. |
| `format` | `enum("Java Block/Item", "geckolib", "animated_java", "Generic Model")` | ❌ | Not specified in schema. | enum: "Java Block/Item", "geckolib", "animated_java", "Generic Model" |
| `name` | `string` | ❌ | Not specified in schema. | No additional constraints declared. |
| `match` | `enum("none", "format", "name", "format_and_name")` | ❌ | Not specified in schema. | enum: "none", "format", "name", "format_and_name" |
| `onMismatch` | `enum("reuse", "error", "create")` | ❌ | Not specified in schema. | enum: "reuse", "error", "create" |
| `onMissing` | `enum("create", "error")` | ❌ | Not specified in schema. | enum: "create", "error" |
| `confirmDiscard` | `boolean` | ❌ | Not specified in schema. | No additional constraints declared. |
| `uvPixelsPerBlock` | `number` | ❌ | Not specified in schema. | No additional constraints declared. |
| `dialog` | `object` | ❌ | Not specified in schema. | No additional constraints declared. |
| `force` | `boolean` | ❌ | Not specified in schema. | No additional constraints declared. |
| `ifRevision` | `string` | ❌ | Not specified in schema. | No additional constraints declared. |
| `includeState` | `boolean` | ❌ | Not specified in schema. | No additional constraints declared. |
| `includeDiff` | `boolean` | ❌ | Not specified in schema. | No additional constraints declared. |
| `diffDetail` | `enum("summary", "full")` | ❌ | Not specified in schema. | enum: "summary", "full" |

## Minimal Request Example
```json
{}
```

## Response Example
This response is a schematic example. Actual values and optional fields vary by project state and runtime version.

```json
{
  "structuredContent": {
    "action": "created",
    "project": {
      "id": "value",
      "format": "Java Block/Item",
      "name": "value"
    }
  }
}
```

## Operational Notes
- Send only fields declared in the input schema.
- Treat the response example as schematic, and handle optional fields defensively.
- For mutating tools, use `ifRevision` when the tool supports it.
