---
title: Export Trace Log
description: "Flushes the trace log to disk (writeFile/export) and returns the resource URI for the in-memory log."
---

## Quick Summary
Flushes the trace log to disk (writeFile/export) and returns the resource URI for the in-memory log.

## Overview
- Tool name: `export_trace_log`
- Category: Project
- Strict schema: `true`

## Tool Description
Flushes the trace log to disk (writeFile/export) and returns the resource URI for the in-memory log.

## Input Schema (JSON)
```json
{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "mode": {
      "type": "string",
      "enum": [
        "auto",
        "writeFile",
        "export"
      ]
    },
    "destPath": {
      "type": "string"
    },
    "fileName": {
      "type": "string"
    }
  }
}
```

## Field Reference
| Field | Type | Required | Description | Constraints |
| --- | --- | --- | --- | --- |
| `mode` | `enum("auto", "writeFile", "export")` | ❌ | Not specified in schema. | enum: "auto", "writeFile", "export" |
| `destPath` | `string` | ❌ | Not specified in schema. | No additional constraints declared. |
| `fileName` | `string` | ❌ | Not specified in schema. | No additional constraints declared. |

## Minimal Request Example
```json
{}
```

## Response Example
This response is a schematic example. Actual values and optional fields vary by project state and runtime version.

```json
{
  "structuredContent": {}
}
```

## Operational Notes
- Send only fields declared in the input schema.
- Treat the response example as schematic, and handle optional fields defensively.
- For mutating tools, use `ifRevision` when the tool supports it.
