---
title: Reload Plugins
description: "Reloads Blockbench plugins (confirm required)."
---

## Quick Summary
Reloads Blockbench plugins (confirm required).

## Overview
- Tool name: `reload_plugins`
- Category: System
- Strict schema: `true`

## Tool Description
Reloads Blockbench plugins (confirm required).

## Input Schema (JSON)
```json
{
  "type": "object",
  "required": [
    "confirm"
  ],
  "additionalProperties": false,
  "properties": {
    "confirm": {
      "type": "boolean"
    },
    "delayMs": {
      "type": "number"
    }
  }
}
```

## Field Reference
| Field | Type | Required | Description | Constraints |
| --- | --- | --- | --- | --- |
| `confirm` | `boolean` | ✅ | Not specified in schema. | No additional constraints declared. |
| `delayMs` | `number` | ❌ | Not specified in schema. | No additional constraints declared. |

## Minimal Request Example
```json
{
  "confirm": true
}
```

## Response Example
This response is a schematic example. Actual values and optional fields vary by project state and runtime version.

```json
{
  "structuredContent": {
    "scheduled": true,
    "delayMs": 0,
    "method": "devReload"
  }
}
```

## Operational Notes
- Send only fields declared in the input schema.
- Treat the response example as schematic, and handle optional fields defensively.
- For mutating tools, use `ifRevision` when the tool supports it.
