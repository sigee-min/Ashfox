---
title: Set Trigger Keyframes
description: "Sets trigger keyframes (sound/particle/timeline), one key per call."
---

## Quick Summary
Sets trigger keyframes (sound/particle/timeline), one key per call.

## Overview
- Tool name: `set_trigger_keyframes`
- Category: Animation
- Strict schema: `true`

## Tool Description
Sets trigger keyframes (sound/particle/timeline), one key per call.

## Input Schema (JSON)
```json
{
  "type": "object",
  "required": [
    "clip",
    "channel",
    "keys"
  ],
  "additionalProperties": false,
  "properties": {
    "clipId": {
      "type": "string"
    },
    "clip": {
      "type": "string"
    },
    "channel": {
      "type": "string",
      "enum": [
        "sound",
        "particle",
        "timeline"
      ]
    },
    "keys": {
      "type": "array",
      "minItems": 1,
      "maxItems": 1,
      "items": {
        "type": "object",
        "required": [
          "time",
          "value"
        ],
        "additionalProperties": false,
        "properties": {
          "time": {
            "type": "number"
          },
          "value": {}
        }
      }
    },
    "ifRevision": {
      "type": "string"
    },
    "includeState": {
      "type": "boolean"
    },
    "includeDiff": {
      "type": "boolean"
    },
    "diffDetail": {
      "type": "string",
      "enum": [
        "summary",
        "full"
      ]
    }
  }
}
```

## Field Reference
| Field | Type | Required | Description | Constraints |
| --- | --- | --- | --- | --- |
| `clipId` | `string` | ❌ | Not specified in schema. | No additional constraints declared. |
| `clip` | `string` | ✅ | Not specified in schema. | No additional constraints declared. |
| `channel` | `enum("sound", "particle", "timeline")` | ✅ | Not specified in schema. | enum: "sound", "particle", "timeline" |
| `keys` | `array<object>` | ✅ | Not specified in schema. | minItems: 1, maxItems: 1 |
| `keys[].time` | `number` | ✅ | Not specified in schema. | No additional constraints declared. |
| `keys[].value` | `any` | ✅ | Not specified in schema. | No additional constraints declared. |
| `ifRevision` | `string` | ❌ | Not specified in schema. | No additional constraints declared. |
| `includeState` | `boolean` | ❌ | Not specified in schema. | No additional constraints declared. |
| `includeDiff` | `boolean` | ❌ | Not specified in schema. | No additional constraints declared. |
| `diffDetail` | `enum("summary", "full")` | ❌ | Not specified in schema. | enum: "summary", "full" |

## Minimal Request Example
```json
{
  "clip": "clip",
  "channel": "timeline",
  "keys": [
    {
      "time": 0,
      "value": "marker"
    }
  ]
}
```

## Response Example
This response is a schematic example. Actual values and optional fields vary by project state and runtime version.

```json
{
  "structuredContent": {
    "clip": "value",
    "channel": "value"
  }
}
```

## Operational Notes
- Send only fields declared in the input schema.
- Treat the response example as schematic, and handle optional fields defensively.
- For mutating tools, use `ifRevision` when the tool supports it.
