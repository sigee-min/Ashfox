---
title: Create Animation Clip
description: "Creates an animation clip (low-level)."
---

## Quick Summary
Creates an animation clip (low-level).

## Overview
- Tool name: `create_animation_clip`
- Category: Animation
- Strict schema: `true`

## Tool Description
Creates an animation clip (low-level).

## Input Schema (JSON)
```json
{
  "type": "object",
  "required": [
    "name",
    "length",
    "loop",
    "fps"
  ],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string"
    },
    "name": {
      "type": "string"
    },
    "length": {
      "type": "number"
    },
    "loop": {
      "type": "boolean"
    },
    "fps": {
      "type": "number"
    },
    "ifRevision": {
      "type": "string"
    },
    "includeState": {
      "type": "boolean"
    },
    "includeDiff": {
      "type": "boolean"
    },
    "diffDetail": {
      "type": "string",
      "enum": [
        "summary",
        "full"
      ]
    }
  }
}
```

## Field Reference
| Field | Type | Required | Description | Constraints |
| --- | --- | --- | --- | --- |
| `id` | `string` | ❌ | Not specified in schema. | No additional constraints declared. |
| `name` | `string` | ✅ | Not specified in schema. | No additional constraints declared. |
| `length` | `number` | ✅ | Not specified in schema. | No additional constraints declared. |
| `loop` | `boolean` | ✅ | Not specified in schema. | No additional constraints declared. |
| `fps` | `number` | ✅ | Not specified in schema. | No additional constraints declared. |
| `ifRevision` | `string` | ❌ | Not specified in schema. | No additional constraints declared. |
| `includeState` | `boolean` | ❌ | Not specified in schema. | No additional constraints declared. |
| `includeDiff` | `boolean` | ❌ | Not specified in schema. | No additional constraints declared. |
| `diffDetail` | `enum("summary", "full")` | ❌ | Not specified in schema. | enum: "summary", "full" |

## Minimal Request Example
```json
{
  "name": "clip",
  "length": 1,
  "loop": true,
  "fps": 20
}
```

## Response Example
This response is a schematic example. Actual values and optional fields vary by project state and runtime version.

```json
{
  "structuredContent": {
    "id": "value",
    "name": "value"
  }
}
```

## Operational Notes
- Send only fields declared in the input schema.
- Treat the response example as schematic, and handle optional fields defensively.
- For mutating tools, use `ifRevision` when the tool supports it.
