import { INTENT_PROGRAM_LANGUAGE_SPECIFICATION } from '../language';

/** Exact machine policy consumed by every semantic constraint stage. */
export const INTENT_PROGRAM_INVARIANTS =
  INTENT_PROGRAM_LANGUAGE_SPECIFICATION.invariants;
